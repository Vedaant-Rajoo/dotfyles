#!/usr/bin/env bash
# Compatibility stub: tmux is retired, this forwards to herdr-sessionizer.
# Kept so muscle-memory / external callers of `tmux-sessionizer` still work.
# The original tmux implementation lives in git history.
exec "$(dirname "$0")/herdr-sessionizer" "$@"
