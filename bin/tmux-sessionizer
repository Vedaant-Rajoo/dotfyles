#!/usr/bin/env bash
# Soft-replaced by herdr-sessionizer during the Herdr trial.
# Original tmux implementation lives in git history; tmux/ is left unused.
exec "$(dirname "$0")/herdr-sessionizer" "$@"
